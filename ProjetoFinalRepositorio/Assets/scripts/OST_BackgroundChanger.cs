using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OST_BackgroundChanger : MonoBehaviour
{
    public GameObject caveOST, forestOST, castleOST;
    public bool isCave, isForest, isCastle;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
